export interface Hero {
    id: number,
    name: string,
    alterEgo: string
  }

  export interface Friend {
    id: number,
    name: string,
  }

  export interface Color {
    id: number,
    label: string,
  }
